 
arr = list(map(int, input("Enter numbers separated by space: ").split()))
temp = []
for i in range(len(arr) - 1, -1, -1):
    temp.append(arr[i])
print("Original Array:", arr)
print("Reversed Array:", temp)
